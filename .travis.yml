python:
  - 2.7
install:
  - pip install zc.buildout
  - buildout -Nc travis.cfg
script:
  - bin/code-analysis
  - bin/test-coverage
after_success:
  - pip install coveralls
  - coveralls
notifications:
  email:
    - tisto@plone.org

