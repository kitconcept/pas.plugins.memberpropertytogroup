python:
  - 2.7
install:
  - python bootstrap-buildout.py -c travis.cfg
  - bin/buildout -Nc travis.cfg
script:
  - bin/code-analysis
  - bin/test-coverage
after_success:
  - pip install coveralls
  - coveralls
notifications:
  email:
    - tisto@plone.org

